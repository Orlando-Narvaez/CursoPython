Crear carpeta: mkdir
crear archivo: touch.txt

str(variables) = pasar a cadena

and = &&
or = ||
not = contrario
==, !=, <, >, <=, >=

round(variable , 2)

variable.upper() = texto en mayuscula
variable.capitalize() = Primera letra en mayuscula
variable.strip() = eliminar espacios
variable.lower() = texto en minuscula
variable.replace('o', 'a') = reemplazar o por la a
variable[0]
len(variable) = para saber cuantos caracteres tiene

separar caracteres: (Slices)
variable[0:8:2] inicial:final:pasos

variable.append(valorAgregar)
variable.pop(indice que quiero borrar)

tuplas son inmutables

